<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/normalize.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= pageTitle %></title>
  </head>
  <body class="bodyContainer">
    <%- include('header') -%>
    <main class="memberEnrollmentParentContainer">
      <h1><%= title %></h1>
      <div class="memberEnrollmentBackgroundForm">
        <form action="" method="POST">
          <div class="memberEnrollFormContainer">
            <div class="labelAndInput">
              <label for="password">Password</label>
              <input
                value="<%= (typeof password === 'undefined') ? '' : password %>"
                name="password"
                id="password"
                type="password"
                required
              />
              <% if (typeof errors !== 'undefined') { %> <%
              errors.forEach(function(error) { %> <% if ((typeof error !==
              'undefined') && error.path === 'password') { %>
              <span class="errorMsg"><%= error.msg %></span>
              <% } %> <% }); %> <% } %>
            </div>

            <button class="joinMembershipBtn" type="submit">Enroll</button>
          </div>
        </form>
        <% if (typeof messageFailure !== 'undefined' && messageFailure.length) {
        %>
        <div class="messageFailure"><%= messageFailure %></div>
        <% } %>
      </div>
    </main>
  </body>
</html>
